{
  "name": "dashboard",
  "packageManager": "yarn@3.1.1",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "yarn workspace server start & yarn workspace client dev",
    "docker:start": "yarn workspace server start & yarn workspace client docker:build"
  }
}
