{
  "name": "dashed",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "develop": "pnpm -r run develop",
    "docker:start": "yarn workspace server start | awk '{ print \"[API]\", $0 }'"
  }
}